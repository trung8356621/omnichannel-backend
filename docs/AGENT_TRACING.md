# Agent Tracing

`AgentTraceService` — `atrace_` / `aspan_`. Correlate conversation/planning/execution/knowledge/automation refs without replacing them.

Span allowlist in `AgentObservabilityCatalog`. No CoT/secrets. Cross-site timeline read fail-closed.
