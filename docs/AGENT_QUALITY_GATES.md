# Agent Quality Gates

Configurable thresholds (skill match, unsafe max, validation pass). Status: passed|warning|failed|insufficient_data.

`canActivateCandidate` public guard — blocks admin activation when not passed; **auto_promotion always false**.
