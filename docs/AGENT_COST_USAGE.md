# Agent Cost & Usage

`AgentCostUsageTracker` reads provider usage metadata when present. Pricing from versioned registry only — if missing, `cost_unknown=true` (never fabricate). Dashboards site-scoped.
