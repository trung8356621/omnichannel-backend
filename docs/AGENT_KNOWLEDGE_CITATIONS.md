# Agent Knowledge Citations

Server assigns `[K1]…[Kn]` handles via `AgentKnowledgeCitationPresenter`.

Model may cite handles only. Fabricated handles rejected.

UI opens knowledge detail by `knowledge_ref` — not raw file paths or model-supplied URLs.
